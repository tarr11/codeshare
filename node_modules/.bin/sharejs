#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../share/bin/sharejs" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../share/bin/sharejs" "$@"
  ret=$?
fi
exit $ret
