#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../share/bin/exampleserver" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../share/bin/exampleserver" "$@"
  ret=$?
fi
exit $ret
